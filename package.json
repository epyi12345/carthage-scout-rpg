{
  "name": "carthage-scout-rpg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run prepare-logo && vite",
    "build": "npm run prepare-logo && tsc -b && vite build",
    "preview": "npm run prepare-logo && vite preview",
    "typecheck": "tsc -b",
    "prepare-logo": "node scripts/prepare-logo.mjs"
  },
  "dependencies": {
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {}
}
