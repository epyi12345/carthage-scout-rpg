# Logo placeholder

Place the launch splash logo here before release:

`logo_ref_heick_games_full.png`

The app references it with Vite's GitHub Pages base path:

`${import.meta.env.BASE_URL}assets/logos/logo_ref_heick_games_full.png`

Until the PNG is added, the splash component falls back to text so the build remains valid.
